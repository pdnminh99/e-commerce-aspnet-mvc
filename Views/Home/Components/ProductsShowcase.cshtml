@using System.Globalization;

@model List<EcommerceApp2259.Models.Product>;

@{
    var size = Model.Count;
    var index = 0;
    var maxItemsPerRow = (int) ViewData["ItemsPerRow"];

    while (index < size)
    {
        <div class="row">
            @{
                for (int col = 0; col < maxItemsPerRow && index < size; col++) 
                {
                    <div class="card product-card">
                        <div style="width: 100%; height: 150px;">
                            <img src="@Model[index].Images" class="card-img-top product-image" />
                        </div>
                        <div class="card-body product-card-body">
                            <p class="product-title">
                                <strong>@Model[index].Title</strong>
                            </p>
                            <p class="product-text">@Model[index].LocaleActualPrice&nbsp;₫</p>
                            <p class="product-text">@Model[index].Sale?.Description</p>
                        </div>
                    </div>
                    index++;
                }
            }
        </div>
    }
}

<style>
    .product-card {
        width: 150px;
        margin: 0 2px;
        border-radius: 5px;
    }

    .product-image {
        border-top-left-radius: 5px;
        border-top-right-radius: 5px;
        max-height: 150px;
    }

    .product-card:hover {
        box-shadow: 0 8px 16px 0 rgba(220,20,60,0.2);
    }

    .product-card-body {
        margin: 0px;
        padding: 0px;
        padding-bottom: 10px;
        padding-left: 10px;
    }

    .product-title {
        font-size: 12px; 
        font-family: sans-serif; 
        font-style: bolder;
        margin: 5px 0;
    }

    .product-text {
        font-size: 14px; 
        color: red; 
        font-family: sans-serif;
        margin: 0;
    }
</style>